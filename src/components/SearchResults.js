// export default function SearchResults({items, query}){

//   query = query.toLowerCase(); // convert query to lower case
  
//   return (
//     <>
//     <h1>Search Results: </h1>
//     {items.filter(item => item.name.split(' ').some(word => word.toLowerCase().startsWith(query)))}
//     </>
//   )
// }

  // we may get results asynchronously by using async-await format.
  // make a request for the data required! 